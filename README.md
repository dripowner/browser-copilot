# Browser Copilot

AI-агент для автономного управления веб-браузером на базе LangGraph и chrome-devtools-mcp.

## Возможности

- 🤖 **Автономное выполнение** сложных многошаговых задач в браузере
- 🧠 **Планирование задач** - агент автоматически составляет план перед выполнением
- 🌐 **Chrome DevTools Protocol** - прямое управление браузером через CDP
- 💾 **Персистентность сессий** - сохраняет cookies и авторизации между запусками
- 🔄 **Гибкий выбор LLM** - работает с любым OpenAI-совместимым API
- ⚡ **Автоматический запуск MCP** - не требует ручного управления MCP сервером

## Примеры задач

### 1. Работа с почтой

```text
Прочитай последние 10 писем в яндекс почте и удали спам
```

### 2. Заказ еды

```text
Закажи BBQ-бургер и картошку фри из того места, откуда я заказывал на прошлой неделе
```

### 3. Поиск вакансий

```text
Найди 3 подходящие вакансии Python разработчика на hh.ru и откликнись с сопроводительным письмом
```

## Установка

### Требования

- Python 3.11+
- Node.js 20+ (для автоматического запуска chrome-devtools-mcp)
- [uv](https://docs.astral.sh/uv/) (для управления зависимостями)
- Microsoft Edge или Google Chrome

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd browser-copilot
```

### 2. Установка зависимостей

```bash
uv sync
```

### 3. Настройка окружения

Создайте `.env` файл на основе примера:

```bash
cp config/.env.example .env
```

Отредактируйте `.env`:

```env
# LLM Configuration
LLM_API_KEY=sk-...                       # Ваш API ключ
LLM_BASE_URL=https://api.openai.com/v1  # или https://openrouter.ai/api/v1
LLM_MODEL=gpt-4                          # или anthropic/claude-3.5-sonnet
LLM_TEMPERATURE=0

# Logging
LOG_LEVEL=INFO
```

### 4. Настройка браузера для подключения (один раз)

Агент работает в вашем обычном браузере с вашими авторизациями и расширениями.

**Создайте ярлык браузера с поддержкой автоматизации:**

1. Правой кнопкой на рабочем столе → **Создать** → **Ярлык**
2. В поле "Расположение объекта" вставьте:

   **Для Edge:**

   ```text
   "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222
   ```

   **Для Chrome:**

   ```text
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222
   ```

3. Название: **Browser (для агента)** или просто **Browser**
4. Нажмите **Готово**

**Теперь используйте этот ярлык для запуска браузера!**

> **Примечание:** Если браузер установлен в другом месте, найдите правильный путь через "Свойства" обычного ярлыка браузера.

## Использование

### Рабочий процесс

#### 1. Откройте браузер через созданный ярлык

Просто двойной клик по ярлыку "Browser (для агента)". Браузер откроется как обычно - можете серфить, логиниться на сайты и т.д.

#### 2. Запустите агента

```bash
uv run python main.py
```

MCP сервер chrome-devtools-mcp запустится автоматически и подключится к вашему открытому браузеру через CDP.

### Интерактивный режим

После запуска агента введите задачу:

```text
Задача: Открой yandex.ru и найди погоду в Москве
```

Агент:

1. Составит план выполнения
2. Выполнит все необходимые действия
3. Предоставит результат

### Команды CLI

- `help` - показать справку с примерами
- `exit` или `quit` - выход из программы

## Архитектура

```text
Host Machine:
  User CLI → LangGraph Agent → LLM (OpenAI-compatible) → Browser Tools → MCP Client
                                                                            ↓
  Browser (с --remote-debugging-port) ←──────────────── chrome-devtools-mcp
                                                        (автозапуск через stdio)
```

**Ключевые компоненты:**

- **LangGraph Command API** - модульная архитектура агента с явным управлением потоком
- **Planning Prompt** - заставляет LLM планировать перед выполнением
- **MCP Integration** - официальная интеграция через langchain-mcp-adapters
- **chrome-devtools-mcp** - MCP сервер для управления браузером через CDP
- **Generic LLM Client** - работает с любым OpenAI-compatible API
- **Автоматический запуск** - MCP сервер запускается автоматически при старте приложения

## Продвинутое использование

### Персистентность сессий и Human-in-the-Loop

Агент работает в вашем обычном браузере с вашим профилем.

**Преимущества:**

- Все ваши авторизации уже работают
- Cookies и localStorage сохраняются автоматически
- Расширения браузера доступны
- Можете вручную помочь агенту (залогиниться, пройти CAPTCHA)
- MCP сервер запускается автоматически - не требует ручного управления

**Workflow с авторизациями:**

1. Агент пытается выполнить задачу
2. Если требуется авторизация - агент сообщит об этом
3. Вы вручную логинитесь в том же браузере
4. Агент продолжает работу

### Смена LLM провайдера

Измените в `.env`:

**OpenAI:**

```env
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4
```

**OpenRouter:**

```env
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=anthropic/claude-3.5-sonnet
```

**Ollama (локально):**

```env
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=llama3
```

### Отладка

**Логи Python агента:**

Установите уровень логирования в `.env`:

```env
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
```

**Логи MCP сервера:**

MCP сервер chrome-devtools-mcp выводит логи в консоль приложения. Логи автоматически включены при запуске.

## Troubleshooting

### MCP не может подключиться к браузеру

**Проблема:** MCP сообщает об ошибке подключения к CDP endpoint.

**Решение:**

1. Убедитесь что браузер запущен через ярлык с `--remote-debugging-port=9222`
2. Проверьте что CDP работает - откройте в другом браузере: `http://localhost:9222/json`
3. Должен вернуться JSON с информацией о браузере
4. Перезапустите приложение (MCP сервер перезапустится автоматически)

### Браузер не запускается с debugging портом

**Проблема:** Браузер отказывается запускаться с флагом `--remote-debugging-port`.

**Решение:**

- Закройте ВСЕ окна браузера перед запуском через ярлык
- Убедитесь что путь к браузеру правильный
- Попробуйте запустить через командную строку для проверки:

  **Edge:**

  ```cmd
  "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222
  ```

  **Chrome:**

  ```cmd
  "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222
  ```

### LLM не отвечает / долго думает

- Проверьте корректность API ключа
- Проверьте доступность API (например, через curl)
- Попробуйте другую модель

## Разработка

См. [CLAUDE.md](CLAUDE.md) для инструкций по разработке.

## Лицензия

MIT
